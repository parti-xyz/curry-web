- if params[:page].present? and params[:page].to_i > 1
  = render 'campaigns/petition/signers', signs: @signs
- else
  = render 'campaigns/petition/section_cover'
  = render 'campaigns/petition/section_tab', menu: 'signer'
  %section.section-panel
    = render 'campaigns/petition/section_right'
    %section.section-left
      %section.section-signer
        .section-title
          현재 #{@campaign.signs.size}명이 참여하고 있습니다.
        = render 'campaigns/petition/signers', signs: @signs
  = render 'campaigns/petition/sign_button'


