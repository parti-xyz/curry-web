<% modal_dom_id = "agent-request-modal-comment-#{SecureRandom.uuid}" %>
var comment_form = "<%= j(render 'campaigns/petition/comment_form', modal_dom_id: modal_dom_id)  %>"
$('body').append(comment_form)
$('#<%= modal_dom_id %>').on('hide.bs.modal', function(e) { $(this).remove() })
$('#<%= modal_dom_id %>').modal()
