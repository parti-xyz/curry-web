= render 'campaigns/special_agenda/section_cover'

= render 'campaigns/special_agenda/section_tab', menu: 'comment'

%section.section-panel
  = render 'campaigns/special_agenda/section_right'

  %section.section-left
    %section.section-comments
      .section-title
        의견을 공유해 보세요
      - page_dom_id = build_uid
      = render 'comments/form_line', commentable: @campaign
      = render 'comments/page_formal', commentable: @campaign, comments: @campaign.comments.recent.page(1), dom_id: page_dom_id

= render 'campaigns/special_agenda/order_button', campaign: @campaign
