%section#sign-up
  .container
    %h3.sign-up-header
      당신의 이름이 될
      %br
      멋진 아이디를 만들어주세요.
    .sign-up-form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        = render 'devise/shared/error_messages', resource: resource
        = f.hidden_field :remember_me
        .form-group
          = f.label :nickname do
            사용할 아이디를 넣어 주세요.
          = f.text_field :nickname, autofocus: true, class: 'form-control'
          .text-primary
            * 한글, 알파벳, 숫자, _ 만 가능합니다.

        - if 'email' == resource.try(:provider)
          .form-group
            = f.label :password
            = f.password_field :password, autofocus: true, class: 'form-control'
            .help-block 최소 #{Devise.password_length.min}자 이상 적어 주세요.
          .form-group
            = f.label :password_confirmation
            = f.password_field :password_confirmation, autofocus: true, class: 'form-control'
        - if 'email' == resource.try(:provider) or resource.email.blank? or resource.errors.include?(:email)
          .form-group
            = f.label :email do
              이메일을 넣어 주세요.
            = f.text_field :email, autofocus: true, class: 'form-control'

        .form-group
          %input{ type: "checkbox", id: "js-checkbox-signup-all" }
          이용약관, 개인정보 수집 및 동의, 마케팅 정보 수신(선택)에 모두 동의합니다.
          %ul
            %li.checkbox-signup
              = f.check_box :term_service, class: "js-checkbox-signup"
              이용약관에 동의합니다. (필수) 
              %a.text-primary{href: '/terms', target: '_blank'}
                %b [보기]
            %li.checkbox-signup
              = f.check_box :term_privacy, class: "js-checkbox-signup"
              개인정보 수집 및 이용에 동의합니다. (필수) 
              %a.text-primary{href: '/privacy', target: '_blank'}
                %b [보기]
            %li.checkbox-signup
              = f.check_box :term_marketing, class: "js-checkbox-signup"
              마케팅 정보 수신에 동의합니다. (선택) [보기]
        .form-group
          = f.submit '회원 가입하기', class: 'btn btn-primary btn-block', data: { disable_with: "진행 중..." }